.agents/
skills-lock.json
scratch.md
.checkpoint/

# Go
*.exe
*.test
*.out
/bin/

# Env
.env.local
.env.*.local

# OS
.DS_Store

# Terraform — local plugin cache + state. Lock files (.terraform.lock.hcl)
# DO get committed (provider version pinning). Bootstrap state stays local
# per ADR 0009 §Implementation Decisions.
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
